本仓库主要针对算法简单复刻
卡尔曼滤波高斯直线效果显著，但若遇到非线性函数演变的扩展与无迹卡尔曼会有更好的效果，ekf核心求出雅可比矩阵拟合线性化，ukf以求概率的思想产生已知点穿越非线性函数，省去了求导的过程，但均是求出卡尔曼增益来概率猜测，找到机器人最可能存在的位置，
A*算法寻求全局最优路径，核心公式f(n)=h(n)+g(n),每部迭代寻求最小的f
mpc控制算法对显示约束具有较好的应对，通过运动模型来预测后n步的运动决策
占据地图算法应用对数概率相加可以极大简化算法，避免下溢
